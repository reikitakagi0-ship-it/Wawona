name: Build iOS xcarchive (Nix, refresh mesa)

on:
  push:
    branches: [ master ]
  pull_request:

jobs:
  build-ios:
    runs-on: macos-26
    env:
      XCODE_APP: /Applications/Xcode.app

    steps:
      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '26.0'

      - name: Checkout
        uses: actions/checkout@v4

      # Xcode を明示的に選択
      - name: Select Xcode
        run: |
          sudo xcode-select -s "$XCODE_APP"
          xcodebuild -version

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v9

      - name: Enable Nix flakes
        run: |
          mkdir -p ~/.config/nix
          echo "experimental-features = nix-command flakes" >> ~/.config/nix/nix.conf

      # FlakeHub は無効化
      - name: Nix cache
        uses: DeterminateSystems/magic-nix-cache-action@v6
        with:
          flakehub: false

      # mesa fixed-output hash mismatch 回避
      - name: Refresh mesa fixed-output hash
        run: |
          nix build .#mesa --refresh -L || true

      # 実機向けビルド（シミュレーターは不要）
      - name: Build waypipe-ios
        run: |
          nix build .#waypipe-ios -L

      # xcarchive を収集
      - name: Collect xcarchive
        run: |
          mkdir -p artifacts
          find result -name "*.xcarchive" -maxdepth 6 -print -exec cp -R {} artifacts/ \;

      - name: Upload xcarchive
        uses: actions/upload-artifact@v4
        with:
          name: waypipe-ios-xcarchive
          path: artifacts/*.xcarchive
