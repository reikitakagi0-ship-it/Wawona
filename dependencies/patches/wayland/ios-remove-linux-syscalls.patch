# Patch: Remove Linux-specific syscalls for iOS
#
# iOS (Darwin) doesn't support signalfd, timerfd, and other Linux-specific
# syscalls. This patch removes or replaces them with compatible alternatives.
#
# This is a placeholder patch file. Replace with actual patch content.
# To generate a patch:
#   1. Clone the repository
#   2. Make your changes
#   3. Run: git diff > ios-remove-linux-syscalls.patch
#
# Example patch content structure:
# --- a/src/event-loop.c
# +++ b/src/event-loop.c
# @@ -123,7 +123,7 @@
#  #ifdef __linux__
#      timerfd_create(...);
#  #else
# -    // Linux-specific code
# +    // Use kqueue/epoll-shim instead
#  #endif
